# FilaOps Environment Configuration
# ====================================
# Copy this file to .env in the repo root and update values
#
# cp .env.example .env

# ===========================================
# DATABASE (Required)
# ===========================================
# PostgreSQL 16+ required
DB_HOST=localhost
DB_PORT=5432
DB_NAME=filaops
DB_USER=postgres
DB_PASSWORD=your-secure-password-here

# ===========================================
# SECURITY (Required)
# ===========================================
# Generate a secure key with:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# ===========================================
# ENVIRONMENT
# ===========================================
# Options: development, production
ENVIRONMENT=development
DEBUG=false
LOG_LEVEL=INFO

# ===========================================
# CORS (Required for frontend)
# ===========================================
# Add your frontend URL(s) here (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

# For remote access, also set your external frontend URL:
# FRONTEND_URL=http://your-domain.com:5173

# ===========================================
# TIER (Community Edition)
# ===========================================
# Options: open, pro, enterprise
# Community edition uses "open"
TIER=open

# ===========================================
# OPTIONAL INTEGRATIONS
# ===========================================
# Google Drive (disabled by default)
ENABLE_GOOGLE_DRIVE=false

# Sentry Error Tracking (optional)
# SENTRY_DSN=https://your-key@your-org.ingest.sentry.io/your-project

# Email/SMTP (see docs/EMAIL_CONFIGURATION.md)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@yourdomain.com
